

<div id='clinicvisit'>
<form class="ui form">

  <!--
  SURGERY INFO
  -->

  <h3 class="ui dividing header">Surgery Information</h3>
    <div class="two fields">
      <div class="field">
        <label>Date of Surgery</label>
          <div class="field">
            {{input type="text" value=dateOfSurgery placeholder="MM-DD-YYYY"}}
          </div>
      </div>
      <div class="field">
        <label>Date of Admission</label>
        <div class="field">
            {{input type="text" value=dateOfAdmission placeholder="MM-DD-YYYY"}}
        </div>
      </div>
    </div>

    <div class="two fields">
      <div class="field">
        <label>Date of Discharge</label>
          <div class="field">
            {{input type="text" value=dateOfDischarge placeholder="MM-DD-YYYY"}}
          </div>
      </div>
      <div class="field">
        <label>Operating Surgeon</label>
        {{ember-selectize
            content=controller.operatingSurgeon
            selection=controller.selectedOperatingSurgeon
            placeholder="Select an item"
            multiple="true"}}
      </div>
    </div>

  <!--
  PROCEDURE DETAILS
  -->

  <h3 class="ui dividing header">Procedure Details</h3>
    <div class="two fields">
      <div class="field">
        <label>Tumor Location(s)</label>
          {{ember-selectize
            content=controller.tumorLocation
            
            selection=controller.selectedTumorLocation
            placeholder="Select an item"
            multiple="true"}}
      </div>
      <div class="field">
        <label>Resection</label>
        {{ember-selectize
            content=controller.resection
            
            selection=controller.selectedResection
            placeholder="Select an item"
            multiple="true"}}
      </div>
    </div>

    <div class="two fields">
      <div class="field">
        <label>Approach</label>
          {{ember-selectize
            content=controller.approach
            
            selection=controller.selectedApproach
            placeholder="Select an item"
            multiple="true"}}
      </div>
      <div class="field">
        <label>Repair Material(s)</label>
        {{ember-selectize
            content=controller.repairMaterial
            
            selection=controller.selectedRepairMaterial
            placeholder="Select an item"
            multiple="true"}}
      </div>
    </div>

    <div class="field">
      <label>Notes</label>
      <textarea></textarea>
    </div>

  <!--
  COMPLICATIONS
  -->

  <h3 class="ui dividing header">Complications</h3>

    <div class="field">
      <label>Complications</label>
        {{ember-selectize
            content=controller.complications
            
            selection=controller.selectedComplications
            placeholder="Select an item"
            multiple="true"}}
    </div>

    <div class="field">
      <label>Notes</label>
      <textarea></textarea>
    </div>

  <div class="ui error message">
    <div class="header">We noticed some issues</div>
  </div>
  {{#link-to 'patient' class="ui grey button"}}
    Cancel
  {{/link-to}}
  <div {{action 'registerSurgery'}}class="ui positive right labeled icon button" style="float: right;">
    Submit<i class="checkmark icon"></i>
  </div>
</form>
</div>